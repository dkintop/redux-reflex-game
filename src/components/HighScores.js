import React, { Component } from "react";

export default class HighScores extends Component {
  render() {
    return (
      <div>
        this page will display the highscores after fetching them from the
        database.
      </div>
    );
  }
}
